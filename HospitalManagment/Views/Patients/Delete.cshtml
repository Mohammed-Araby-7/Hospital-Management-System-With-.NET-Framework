@model Patient

@{
    Layout = null;
    ViewData["Title"] = "حذف المريض";
}

<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

<div class="container py-5" style="max-width: 500px;">
    <div class="card shadow-lg border-0 rounded-4">

        <div class="card-header bg-danger text-white py-3 rounded-top-4">
            <h3 class="mb-0 text-center">حذف المريض</h3>
        </div>

        <div class="card-body px-4 py-4 text-center">
            <p class="fs-5 mb-4">هل أنت متأكد من حذف هذا المريض؟</p>

            <div class="mb-3">
                <p><strong>Id:</strong> @Model.Id</p>
                <p><strong>الاسم:</strong> @Model.Name</p>
                <p><strong>رقم الهاتف:</strong> @Model.Phone</p>
                <p><strong>الدكتور:</strong> @Model.Doctor?.Name</p>
            </div>

            <form asp-action="DeleteConfirmed" method="post">
                <input type="hidden" asp-for="Id" />

                <div class="d-flex justify-content-between">
                    <button type="submit" class="btn btn-danger w-45 py-2 rounded-3 fw-semibold">
                        حذف
                    </button>
                    <a asp-controller="Patients" asp-action="Index" class="btn btn-secondary w-45 py-2 rounded-3 fw-semibold">
                        إلغاء
                    </a>
                </div>
            </form>
        </div>
    </div>
</div>
